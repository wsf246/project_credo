<% provide(:title, 'New Point') %>
<div class= 'question-wrapper'>
	<h2><%=@question.question%></h2>
</div>

<%= simple_form_for([@question, @point]) do |f| %>
	<%= render 'point_form', f: f %>
<%end%>
