<div class ='point-wrapper'>	
	<div class="row-fluid">
		<div class= 'point'>		
		  <div class="col-md-6 col-md-offset-2">
		  	<b><%= link_to "Back to question", question_path(@question) %></b>
		    <% @evidence.each do |evidence|%>     	  	    	
			 		<%= render 'questions/evidence/points', point: evidence %>
			 	<%end%>
			 	<b><%= link_to "Back to question", question_path(@question) %></b>	
		  </div>
		</div>
	</div> 
</div>